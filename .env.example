# Copy to .env and fill in real values. Never commit .env.

DATABASE_URL=postgresql+asyncpg://postgres:postgres@localhost:5432/region_price_monitor
SCHEDULE_CRON=0 */6 * * *
MAX_CONCURRENCY=5
RETRY_LIMIT=3

# Regional proxy provider (ADR-0001) — filled in Фаза 3
PROXY_PROVIDER=
PROXY_API_KEY=
