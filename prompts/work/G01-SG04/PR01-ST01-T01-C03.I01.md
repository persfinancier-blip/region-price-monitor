# G01.SG04.PR01.ST01.T01.C03.I01

## Identity
- Goal: G01
- Subgoal: G01.SG04 — Ozon Proxy-First Regional Price
- Process: G01.SG04.PR01 — Current Ozon regional/session evidence and autonomous-engine contract
- Stage: G01.SG04.PR01.ST01 — Fresh-session HTTP/hidden-browser evidence
- Task: G01.SG04.PR01.ST01.T01 / Issue #71
- Cycle: C03
- Iteration: I01
- Supersedes for execution: `G01.SG04.PR01.ST01.T01.C02.I01`
- Historical prompts remain immutable.

## Runtime finding requiring C03
SG02 proxy transport is now proven on the operator Windows host: native curl, Requests and curl_cffi all reach the same Novosibirsk mobile egress through the explicit HTTPS ASocks proxy. The C02 Ozon probe then received HTTP 403 on a fresh `GET https://www.ozon.ru/` using an Edge impersonation.

Repository archive evidence shows that the previously successful ASocks/Ozon path did not use that request shape. It used the Ozon composer API directly through `curl_cffi` with Chrome impersonation and browser-like same-origin headers; historical live evidence records HTTP 200 and a parsed price. Therefore the next evidence step is to replay the archived proven HTTP shape through the accepted SG02 ProxyContext before introducing a new browser mechanism.

## Input Contract
- accepted SG02 ProxyContext with neutral egress confirmation;
- Ozon SKU, preferably historical control `3129447770` for first replay;
- no SG05 personalized authenticated profile/cookie prerequisite;
- archived proven composer request shape as evidence input, not as semantic authority beyond what runtime re-proves.

## Actions
1. Call `https://www.ozon.ru/api/composer-api.bx/page/json/v2` directly with `url=/product/<SKU>/`.
2. Use `curl_cffi` `impersonate="chrome"` through the same ProxyContext.
3. Reproduce the archived safe request headers required for the composer request (`Accept: application/json`, Russian language preference, product Referer, `x-o3-app-name: dweb_client`, browser sec-fetch/sec-ch hints).
4. Do not warm the homepage first and do not inject legacy/personalized cookies in this replay.
5. Save response body only under Git-ignored local probes; safe report records HTTP outcome, hash, JSON/widget keys and bounded parsed price fields.
6. If composer returns 403/non-JSON/challenge without cookies, classify that as evidence that HTTP-alone is insufficient and hand off to the already-compiled automatic hidden/headless browser bootstrap path; do not attempt manual login, PVZ selection, Enter or captcha solving.

## Output Contract
Either:
- current composer API evidence with `webPrice`/`webSale` widget price fields, still requiring requested-city verification; or
- explicit `OZON_COMPOSER_NO_COOKIE_BLOCKED_BROWSER_BOOTSTRAP_REQUIRED` / `OZON_CONTEXT_CONTRACT_UNPROVEN` with safe response evidence.

## Acceptance Criteria
- exact archived composer request shape is replayed through SG02 ProxyContext;
- no homepage prerequisite obscures the result;
- no personalized legacy cookies/profile are used;
- response classification distinguishes HTTP block, non-JSON/anti-bot, valid composer JSON with/without price widgets;
- no city is considered verified merely because proxy egress is Novosibirsk;
- browser bootstrap is only the next step if this replay proves it necessary.

## Failure Conditions
- direct/no-proxy fallback;
- returning to manual login/PVZ/captcha primary flow;
- treating historical HTTP 200 as current proof without a fresh replay;
- silently switching into SG05 legacy authenticated fallback;
- widening into final collector/persistence/scheduler before evidence is compiled.
