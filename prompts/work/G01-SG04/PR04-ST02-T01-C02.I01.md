# G01.SG04.PR04.ST02.T01.C02.I01

## Role
Execute the repaired SG04 C02 acceptance/reverse-assembly gate. Do not weaken evidence to force PASS.

## Goal
Prove that Ozon regional-price operation is autonomous, proxy-first, city-verified and repeatable without **human** browser work, while allowing hidden/headless Selenium/Chrome as an automatic primary engine.

## Inputs
All accepted SG04 C02 Task outputs and evidence.

## Actions
1. Map every Issue #34 acceptance clause to exact deterministic/runtime evidence.
2. Prove a fresh first run starts without legacy cookies/profile and can obtain a verified-city price or explicit typed failure using the accepted autonomous engine strategy.
3. Prove hidden/headless Selenium/Chrome, when used, requires zero human interaction and derives its proxy configuration from the same SG02 ProxyContext authority.
4. Prove a repeated run can reuse/rebootstrap/refresh automatically, including browser lifecycle cleanup/recreation where applicable.
5. Prove wrong/unverified city cannot yield successful price.
6. Prove challenges requiring user action remain typed primary failures and do not implicitly invoke SG05.
7. Reverse-compose `C02 Prompt → Task → Stage → Process → SG04 → G01 contribution` and check SG05 ownership remains manual/explicit fallback only.

## Verdicts
- `SEMANTIC_COMPILE_PASS` only when the repaired runtime engine/context contract and all acceptance evidence are proven;
- `OZON_CONTEXT_CONTRACT_UNPROVEN` when autonomous engine requirements remain unproven;
- `OZON_BROWSER_PROXY_BINDING_UNPROVEN` when hidden browser cannot be proven bound to requested-city ProxyContext;
- `OZON_REGION_CONTEXT_UNPROVEN` when effective requested city cannot be proven under current minimum CityRecord;
- `STRUCTURAL_REPAIR_REQUIRED` for decomposition/interface mismatch.

## Acceptance
No manual warming hidden in setup, no direct-network fallback, no valid-looking price on failure, hidden browser allowed but zero-interaction, no scope absorption from SG05/SG06.

## Output Contract
Exact C02 test/probe evidence plus acceptance map and final repaired SG04 runtime verdict.