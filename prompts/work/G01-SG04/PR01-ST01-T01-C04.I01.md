# G01.SG04.PR01.ST01.T01.C04.I01

## Role
Continue only the fresh-session Ozon runtime-evidence Task after C03 proved the archived direct composer API request (`curl_cffi`, `impersonate="chrome"`, no cookies) still returns HTTP 403 through a proven Novosibirsk mobile ProxyContext. Preserve C01-C03 as immutable history.

## Goal
Capture zero-human hidden/headless Chrome evidence through the same SG02 ProxyContext and determine whether an automatically created browser session is sufficient to reach Ozon composer/product state and expose a requested-city verification signal.

## Input
- accepted SG02 ProxyContext;
- C03 live evidence: native curl + Requests + curl_cffi share one Novosibirsk mobile egress; direct Ozon composer no-cookie request returns HTTP 403;
- existing Selenium/undetected-chromedriver runtime dependency;
- archived Ozon composer request headers and control SKU `3129447770` as diagnostic controls only.

## Actions
1. Adapt the same ProxyContext into hidden/headless Chrome without creating a second proxy authority. Because browser proxy auth must be automatic, a bounded loopback bridge may translate local unauthenticated CONNECT into the upstream ProxyContext HTTPS/HTTP proxy + Basic auth; the bridge must contain no independent credentials/config.
2. Before touching Ozon, navigate the hidden browser to `https://api.i.pn/json/` and require its observed egress identity to match the already-proven neutral ProxyContext identity. Mismatch/unavailable -> `OZON_BROWSER_PROXY_BINDING_UNPROVEN` and no Ozon navigation.
3. Start with a fresh temporary browser profile and no legacy cookies/profile.
4. Navigate automatically to the control Ozon product and attempt same-origin composer fetch with browser-created session/cookies only.
5. Record sanitized browser startup/navigation status, browser egress identity, cookie names only, bounded challenge markers, composer HTTP/JSON/price-widget evidence, and any bounded requested-city marker found in Ozon content. Raw bodies may remain only under Git-ignored local probes.
6. Never wait for human captcha/login/city/PVZ/Enter. A challenge requiring human action is a typed terminal evidence result.
7. Bound browser/bridge lifetime and clean up processes/sockets on every path.

## Output Contract
Sanitized hidden-browser evidence establishing browser ProxyContext binding and one of: autonomous Ozon bootstrap evidence, human-action-required challenge, browser/session/navigation failure, or context/city verification still unproven.

## Acceptance
- fresh profile; no legacy cookies;
- zero human interaction;
- browser egress proven through the same ProxyContext before Ozon;
- no direct browser fallback;
- no credentials/cookie values in safe output;
- successful price evidence alone does not prove requested city unless an Ozon-side city/context signal is observed.

## Failure Boundary
Do not implement production OzonContext/collector yet; do not weaken city verification; do not introduce SG05 manual fallback.