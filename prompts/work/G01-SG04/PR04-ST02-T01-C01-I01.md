# G01.SG04.PR04.ST02.T01.C01.I01

## Role
Execute the SG04 acceptance/reverse-assembly gate. Do not weaken evidence to force PASS.

## Goal
Prove that Ozon regional-price operation is autonomous, proxy-first, city-verified and repeatable without manually warmed cookies/profile.

## Inputs
All accepted SG04 Task outputs and evidence.

## Actions
1. Map every Issue #34 acceptance clause to exact deterministic evidence.
2. Prove a fresh first run begins without legacy cookies/profile and can obtain a verified-city price or explicit typed failure.
3. Prove a repeated run can reuse/rebootstrap/refresh automatically without human browser action.
4. Prove all normal-path network calls use the requested city's ProxyContext.
5. Prove wrong/unverified city cannot yield a successful price.
6. Reverse-compose Prompt → Task → Stage → Process → SG04 → G01 contribution.

## Verdicts
- `SEMANTIC_COMPILE_PASS` only when the runtime context contract and all acceptance evidence are proven;
- `OZON_CONTEXT_CONTRACT_UNPROVEN` when fresh-session requirements remain unproven;
- `OZON_REGION_CONTEXT_UNPROVEN` when effective requested city cannot be proven under the current minimum CityRecord;
- `STRUCTURAL_REPAIR_REQUIRED` for decomposition/interface mismatch.

## Acceptance
No manual warming hidden in setup, no proxy bypass/direct fallback, no valid-looking price on failure, no scope absorption from SG05/SG06.

## Output Contract
Exact test/probe evidence plus acceptance map and final SG04 runtime verdict.