# G01.SG04.PR01.ST02.T01.C01.I01

## Role
Compile the accepted current Ozon evidence into one explicit regional bootstrap/verification contract. Do not widen the Goal contract silently.

## Goal
Define exactly how the primary Ozon path can establish and verify requested-city context without manual warming.

## Input
Accepted evidence from `G01.SG04.PR01.ST01.T01`.

## Actions
1. State whether fresh proxy-only session is sufficient or automatic bootstrap state is required.
2. Identify only evidenced cookies/headers/page/session state that may be created automatically.
3. Define the exact acceptance signal for effective requested-city binding.
4. Define typed failures for context mismatch, context unproven and bootstrap/session rejection.
5. If current Ozon behavior cannot verify requested city from the minimum CityRecord, output `OZON_REGION_CONTEXT_UNPROVEN` and identify the missing semantic input; do not invent a field or accept proxy geolocation as proof without evidence.

## Output Contract
One deterministic bootstrap/context-verification contract, or fail-closed verdict `OZON_CONTEXT_CONTRACT_UNPROVEN` / `OZON_REGION_CONTEXT_UNPROVEN`.

## Acceptance
- no manual login, city/PVZ selection or browser profile in primary contract;
- no hidden additional mandatory CityRecord field;
- every rule traces to accepted observed evidence;
- wrong/unverified region cannot be accepted as a price success.

## Failure Boundary
No networking implementation, price parsing, collector integration, persistence, scheduler or legacy fallback redesign.