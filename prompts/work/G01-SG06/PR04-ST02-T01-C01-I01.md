# G01.SG06.PR04.ST02.T01.C01.I01

## Role
Execute the SG06 acceptance/reverse-assembly gate only. Do not widen child Tasks to manufacture PASS.

## Goal
Prove that the nine SG06 Tasks compose exactly into Issue #36 and its contribution to G01, with every parent clause backed by an explicit producer/evidence path.

## Inputs
All accepted SG06 Task outputs/evidence and active SG01–SG05 interface contracts.

## Actions
1. Map every Issue #36 acceptance clause to exact Task output/evidence.
2. Reverse-compose `Prompt -> Task -> Stage -> Process -> SG06`.
3. Check SG06 consumes SG01 ProductSet/CitySet without legacy-region substitution.
4. Check SG02 ProxyContext failures survive into terminal rows.
5. Check SG03 WB price/stock/optional-dest outcomes survive accounting and persistence.
6. Check SG04 new proxy-first Ozon outcomes survive accounting/persistence without acquiring SG05 personalized-cookie dependency.
7. Check SG05 remains explicit reserve only and is never an implicit primary repair.
8. Verify matrix, file/PG persistence, run identity, repeat-run and scheduler-ready responsibilities have no orphan.
9. Emit exact fail-closed verdict when any interface/acceptance clause is missing.

## Verdicts
- `SEMANTIC_COMPILE_PASS` when SG06 is compositionally complete;
- `RUNPLAN_ACCOUNTING_INCOMPLETE`;
- `PERSISTENCE_SEMANTIC_LOSS`;
- `PRIMARY_NOT_NONINTERACTIVE`;
- `CROSS_SG_INTERFACE_MISMATCH`;
- `STRUCTURAL_REPAIR_REQUIRED`.

## Output Contract
SG06 compile/acceptance report with reverse-assembly map and explicit handoff to the separate whole-G01 semantic compilation gate.

## Acceptance
No Issue #36 clause orphaned; no child output unused without justification; no SG01–SG05 interface weakened; no runtime evidence is falsely claimed if it remains point-of-use/deferred.

## Failure Boundary
If a Task boundary or parent contract is wrong, stop with `STRUCTURAL_REPAIR_REQUIRED`; do not merely widen this Prompt.