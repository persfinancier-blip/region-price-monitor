# ytim-style immutable initial prompt

Identity: `G01.SG05.PR03.ST02.T01.C01.I01`
Parent Task: `G01.SG05.PR03.ST02.T01`
Parent Subgoal: G01.SG05 / Issue #35
Repository: `persfinancier-blip/region-price-monitor`
Depends on: all accepted SG05 Tasks

## Goal
Provide deterministic SG05 acceptance evidence and reverse composition into G01.

## Output contract
Acceptance-to-evidence map proving:
- usable legacy path not deleted;
- fallback intentionally invokable;
- primary needs no fallback preparation;
- primary CityRecord has no profile/cookie requirement;
- no automatic browser fallback in primary;
- manual WB dest discovery remains optional support;
- regression coverage detects fallback breakage and primary recoupling.

## Reverse assembly
Prove `Prompt → Task → Stage → Process → SG05 → G01` contribution to:
- A09 fallback preservation;
- A01/A11 no manual legacy preparation in normal/repeated primary operation;
- A08 optional `wb_dest` remains valid.

## Verdicts
- `PASS` when all SG05 acceptance clauses have evidence;
- `LEGACY_FALLBACK_RUNTIME_PROBE_REQUIRED` only when deterministic tests prove reachability but a visible desktop browser smoke still must run in an appropriate environment;
- `PRIMARY_LEGACY_RECOUPLED` if primary invokes legacy setup/fallback implicitly;
- `STRUCTURAL_REPAIR_REQUIRED` for wrong Task/interface boundaries.

## Scope
Do not implement SG06 persistence/orchestration/scheduling or automatic fallback selection.
