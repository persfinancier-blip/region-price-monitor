# G01.SG05.PR03.ST02.T01.C02.I01

## Role
Execute SG05 C02 acceptance and reverse-assembly gate.

## Goal
Prove the current authenticated legacy Ozon mechanism is preserved as explicit fallback while the new proxy-first primary remains independent, and reverse-compose SG05 into G01.

## Inputs
All accepted SG05 C02 outputs/evidence + SG04 primary contract.

## Actions
1. Map every Issue #35 acceptance clause to exact evidence.
2. Prove personalized authenticated Ozon cookies/tokens required by the current fallback are preserved as secret runtime material.
3. Prove primary SG04 has no requirement for those cookies/profile and never auto-enters legacy fallback.
4. Prove invalid/expired legacy auth is explicit and not silently downgraded to anonymous cookies.
5. Prove optional WB legacy dest tooling remains available without becoming primary-required.
6. Reverse-compose C02 Prompt → Task → Stage → Process → SG05 → G01.

## Verdicts
- `SEMANTIC_COMPILE_PASS` when deterministic SG05 evidence is complete;
- `LEGACY_FALLBACK_RUNTIME_PROBE_REQUIRED` when structure/reachability is proven but a live desktop authenticated-cookie smoke remains pending;
- `LEGACY_AUTH_COOKIE_SEMANTICS_LOST` if personalized authenticated cookies are replaced/ignored;
- `PRIMARY_LEGACY_RECOUPLED` if primary requires legacy profile/cookies or auto-enters fallback;
- `STRUCTURAL_REPAIR_REQUIRED` for wrong decomposition/interface boundaries.

## Acceptance
Preserves current authenticated fallback, keeps secrets out of Git/log/results/evidence, preserves primary independence and reconstructs G01.A09/A01/A08/A11 contribution.

## Output Contract
Exact acceptance map, runtime-smoke status, and final SG05 C02 reverse-compilation verdict.