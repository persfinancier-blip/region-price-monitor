# ytim-style immutable initial prompt

Identity: `G01.SG05.PR02.ST01.T01.C01.I01`
Parent Task: `G01.SG05.PR02.ST01.T01`
Parent Subgoal: G01.SG05 / Issue #35
Repository: `persfinancier-blip/region-price-monitor`
Depends on: accepted PR01 boundary

## Goal
Preserve an intentionally invokable Ozon legacy fallback path using existing profile/cookies/browser machinery without allowing it to remain an implicit primary dependency.

## Output contract
An explicit fallback capability that can:
- load/use an existing Ozon legacy profile/cookies artifact;
- invoke preserved browser fallback behavior when the operator explicitly selected legacy/fallback mode;
- return success/failure with explicit fallback provenance;
- remain separate from SG04 automatic OzonContext.

## Acceptance
No automatic browser launch from primary Ozon failure; existing legacy assets stay usable; fallback entrypoint is reachable intentionally; fallback does not mutate primary CityRecord requirements; secret/profile paths are not exposed unnecessarily in normalized primary outputs.

## Evidence
Invocation-boundary tests/spies + regression fixtures around explicit fallback selection. Desktop browser smoke may be environment-gated but must have a defined evidence command/procedure.

## Failure conditions
Dead fallback code, implicit fallback, or SG04 primary bootstrap rewritten inside this Task -> `STRUCTURAL_REPAIR_REQUIRED`.

## Scope
Ozon legacy fallback only. Do not implement WB primary behavior, scheduler, persistence or automatic fallback policy.
