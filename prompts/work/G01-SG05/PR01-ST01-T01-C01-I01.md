# ytim-style immutable initial prompt

Identity: `G01.SG05.PR01.ST01.T01.C01.I01`
Parent Task: `G01.SG05.PR01.ST01.T01`
Parent Subgoal: G01.SG05 / Issue #35
Repository: `persfinancier-blip/region-price-monitor`

## Goal
Inventory every existing legacy regional browser/profile/cookies/WB-dest capability and artifact that must remain available after proxy-first migration.

## Read
- `parser/core/cli.py`
- `parser/core/warm_browser.py`
- `parser/core/ozon.py`
- relevant config/profile path helpers only
- `docs/development/G01/SG05/vertical.md`

## Output contract
Produce a deterministic inventory covering at minimum:
- `review_profiles`
- `repair_regions`
- `add_new_regions`
- `warm_region`
- `browser_fetch_prices`
- `get_wb_dest`
- `load_cookies`
- `ozon_profile_dir`, profile directories and `cookies.json`
- legacy `wb_dest` acquisition/validation behavior
- current automatic browser fallback call site(s)

Classify each item as: preserve explicit fallback/support, primary dependency to remove, or internal artifact of preserved legacy path.

## Acceptance
No useful legacy capability is omitted; no new implementation is started; no legacy field is added to minimum CityRecord; exact current call sites are evidenced.

## Failure conditions
If the inventory cannot distinguish primary prerequisite from reserve capability, return `LEGACY_BOUNDARY_UNPROVEN` and stop.

## Scope
Read-only analysis/documentation/tests plan only. Do not implement SG02–SG04 behavior, persistence, scheduler or automatic fallback policy.
