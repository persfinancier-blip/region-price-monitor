# Prompt — region-price-monitor.G01.SG01.PR03.ST02.T01.C01.I01

## Identity
Task: `G01.SG01.PR03.ST02.T01` — Prove SG01 source parity and minimum contract  
Parent: G01.SG01 / Issue #31

## Goal
Close SG01 with deterministic evidence that products and cities can be supplied from file or PostgreSQL through one normalized boundary, while the city contract remains minimum sufficient and `wb_dest` stays optional.

## Prerequisite contracts
All prior SG01 Tasks accepted.

## Read first
- `docs/development/G01/goal.md`
- `docs/development/G01/subgoals.md`
- `docs/development/G01/SG01/vertical.md`
- all accepted SG01 source modules/tests

## Required output
A compact deterministic SG01 acceptance suite/evidence covering every SG01 acceptance item and guarding its boundary against regression.

## Required checks
1. product file source;
2. product DB source;
3. city file source;
4. city DB source;
5. equivalent source fixtures normalize identically;
6. each missing required city field fails explicitly;
7. omitted/blank `wb_dest` succeeds and normalizes to no forced destination;
8. a new product/city fixture can be added without production-code edits;
9. no additional city field is required;
10. new CitySet is not forced through legacy `review_profiles`, `repair_regions` or `add_new_regions` as a prerequisite to exist;
11. no secret test credential is emitted into ordinary test output/log assertions.

## Allowed write scope
- SG01 tests/fixtures;
- minimal corrections only inside production files already authorized by earlier SG01 Tasks when the acceptance suite exposes a direct SG01 contract mismatch.

No WB/Ozon collector, proxy networking, scheduler or result persistence changes.

## Acceptance
All SG01 acceptance checks pass. Any missing child output or cross-interface mismatch is reported as structural/contract failure rather than hidden by weakening a test.

## Evidence
Return test command(s), exact pass/fail result, mapping from each SG01 acceptance item to test/evidence, and exact changed files.

## Stop / fail closed
If failure belongs to SG02/SG03/SG04/SG05/SG06, report the exact dependency and leave SG01 scope unchanged. If an SG01 Task boundary itself is wrong, require canonical decomposition repair before a new Prompt iteration.
