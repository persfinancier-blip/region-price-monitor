# Prompt — region-price-monitor.G01.SG01.PR02.ST01.T01.C01.I01

## Identity
Task: `G01.SG01.PR02.ST01.T01` — Define minimum CityRecord normalization  
Parent: G01.SG01 / Issue #31  
Mode: implementation Prompt, initial cycle/iteration only after full G01 Semantic Compilation authorizes development.

## Goal
Implement the exact minimum city input contract agreed for G01 without adding mandatory fields.

## Read first
- `docs/development/G01/goal.md`
- `docs/development/G01/subgoals.md`
- `docs/development/G01/SG01/vertical.md`

## Input contract
Raw city mapping with:
- `city` required;
- `proxy` required;
- `proxy_user` required;
- `proxy_password` required;
- `wb_dest` optional.

## Required output
One small reusable normalizer/model returning a canonical city record. Required fields must be non-empty after normalization. Missing or blank `wb_dest` must normalize to `None` and must not be an error.

Do not require `proxy_id`, provider, country, region, rotation URL, session ID, priority or any other field.

## Actions
1. Implement the smallest normalization/validation boundary sufficient for downstream source adapters.
2. Keep `proxy` as the user-supplied connection address; do not reinterpret proxy transport here.
3. Normalize `wb_dest` only enough to preserve an explicitly supplied value or represent absence as `None`.
4. Add tests for valid rows, missing each required field, blank required values, missing `wb_dest` and blank `wb_dest`.
5. Use only fake credentials in tests.

## Allowed write scope
- optional `parser/core/input_models.py`
- city-contract tests/fixtures only

## Acceptance
- exact four required proxy/city fields;
- exactly one optional `wb_dest` field;
- missing/blank `wb_dest` accepted;
- explicit validation error for each missing required field;
- no network/browser behavior introduced.

## Evidence
Return changed files, tests and a compact input/output example using dummy credentials.

## Stop / fail closed
If a new required field appears necessary, stop and report the concrete dependency. Do not silently widen the G01 city contract.
