# Prompt — region-price-monitor.G01.SG01.PR02.ST02.T01.C01.I01

## Identity
Task: `G01.SG01.PR02.ST02.T01` — Load cities from file  
Parent: G01.SG01 / Issue #31

## Goal
Add a file-based city source analogous to the existing product file source, returning canonical `CitySet` records and requiring only the agreed five columns.

## Prerequisite contract
`G01.SG01.PR02.ST01.T01` provides the canonical CityRecord normalizer.

## Read first
- `docs/development/G01/SG01/vertical.md`
- `parser/core/cli.py`
- `parser/core/config.py`
- the accepted output/interface of PR02.ST01.T01

## Input contract
CSV or Excel file whose columns are:
- `city`
- `proxy`
- `proxy_user`
- `proxy_password`
- optional `wb_dest`

Column name matching may be case-normalized consistently with the existing product loader. Do not invent required columns.

## Required output
A city file loader returning canonical `CitySet` through the PR02.ST01 normalizer.

A sample/fixture may be added with dummy credentials only.

## Actions
1. Reuse the existing product file-loading style where practical.
2. Support CSV and Excel if the current dependency set already supports them; do not add a large framework.
3. Normalize every row through the canonical CityRecord contract.
4. Produce an explicit file/row validation error instead of silently dropping invalid city rows.
5. Add tests for valid multiple-city file, missing required column/value, omitted `wb_dest`, and equivalent CSV/Excel semantics where supported.
6. Do not connect to proxies or marketplaces.

## Allowed write scope
- `parser/core/config.py`
- optional `parser/core/city_sources.py`
- parser-visible dummy sample city file if useful
- city-file tests/fixtures

## Acceptance
- city file source works;
- no source-code edit is needed to add/change a city row;
- missing `wb_dest` works;
- invalid required data fails explicitly;
- output contains no file-specific shape.

## Evidence
Return changed files, tests/results and one normalized dummy CitySet example.

## Stop / fail closed
Do not wire the new CitySet through legacy `review_profiles`, `repair_regions`, `add_new_regions` or marketplace parsing in this Task. That would cross SG01 boundaries and could reintroduce mandatory legacy `dest`/profile semantics.
