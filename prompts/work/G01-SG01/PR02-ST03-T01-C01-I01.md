# Prompt — region-price-monitor.G01.SG01.PR02.ST03.T01.C01.I01

## Identity
Task: `G01.SG01.PR02.ST03.T01` — Load cities from PostgreSQL  
Parent: G01.SG01 / Issue #31

## Goal
Add a minimal PostgreSQL city source that stores/loads the same user data as the city file source and returns the same canonical `CitySet`.

## Prerequisite contract
`G01.SG01.PR02.ST01.T01` canonical CityRecord normalization.

## Read first
- `docs/development/G01/SG01/vertical.md`
- `parser/core/db.py`
- accepted CityRecord interface from PR02.ST01.T01

## Input contract
Existing ParserDB PostgreSQL connection and city rows containing only:
- `city`
- `proxy`
- `proxy_user`
- `proxy_password`
- optional `wb_dest`

## Required output
A minimal `parser_cities` table/read path and `load_cities()`-equivalent API returning canonical `CitySet`.

The table may use DB-internal constraints/indexes, but must not create additional mandatory user-supplied data. Prefer `city` as the stable unique key unless current DB constraints prove another generated key is necessary; generated keys must not enter the user contract.

## Actions
1. Extend existing ParserDB minimally; do not introduce a second DB abstraction.
2. Create/migrate `parser_cities` safely using current idempotent schema style.
3. Load rows and pass every row through the canonical CityRecord normalizer.
4. Add tests for schema/read behavior, multiple cities, optional `wb_dest`, invalid required values and semantic parity with a city-file fixture.
5. Use dummy credentials only.

## Allowed write scope
- `parser/core/db.py`
- optional `parser/core/city_sources.py`
- city-DB tests/fixtures

## Acceptance
- PostgreSQL city source works;
- same semantic city rows as file normalize identically;
- no new mandatory user field;
- missing `wb_dest` is valid;
- DB failure/invalid rows are explicit, not empty-success.

## Evidence
Return changed files, tests/results, effective table schema and parity evidence.

## Stop / fail closed
Do not implement proxy networking, collectors, result storage changes or legacy profile integration here.
