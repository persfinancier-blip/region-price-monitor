# Prompt — region-price-monitor.G01.SG01.PR01.ST01.T01.C01.I01

## Identity
Task: `G01.SG01.PR01.ST01.T01` — Normalize and preserve product sources  
Parent: G01.SG01 / Issue #31  
Mode: implementation Prompt, initial cycle/iteration only after full G01 Semantic Compilation authorizes development.

## Goal
Preserve the current product input capabilities while creating/confirming one canonical `ProductSet` boundary that is identical for file and PostgreSQL origins.

## Read first
- `docs/development/G01/goal.md`
- `docs/development/G01/subgoals.md`
- `docs/development/G01/SG01/vertical.md`
- `parser/core/cli.py`
- `parser/core/db.py`
- `parser/core/config.py`

## Input contract
Existing product sources:
- CSV/Excel rows with `marketplace`, `sku`;
- PostgreSQL `parser_skus`;
- preserve existing `products.json` compatibility unless a concrete regression proves it must change.

## Required output
A tested canonical result:

```python
{"wb": ["..."], "ozon": ["..."]}
```

with normalized string SKUs and accepted marketplace values limited to current supported marketplaces.

Equivalent semantic file/DB fixtures must normalize identically.

## Actions
1. Inspect the current loaders before changing them; reuse working behavior.
2. Extract/refactor only as needed to expose a source-neutral product boundary.
3. Preserve current user-facing file/DB behavior unless a change is necessary to satisfy this Task contract.
4. Add deterministic tests for file normalization, DB normalization, equivalent-output parity and invalid marketplace/input handling.
5. Do not add city/proxy behavior.

## Allowed write scope
- `parser/core/cli.py`
- `parser/core/db.py`
- `parser/core/config.py`
- optional `parser/core/input_models.py`
- optional `parser/core/product_sources.py`
- product-source tests/fixtures

No other production path is authorized.

## Acceptance
- file product source passes;
- DB product source passes;
- equivalent fixtures produce equivalent `ProductSet`;
- SKU representation is consistent;
- current supported file behavior is not unnecessarily removed;
- no product identifiers are hardcoded into implementation.

## Evidence
Return exact changed files, tests executed and results. Explain the canonical ProductSet interface and show parity evidence.

## Stop / fail closed
Stop with a typed blocker rather than widening scope if the required output needs changes to marketplace collectors, proxy transport, city handling, result persistence or scheduler behavior.
