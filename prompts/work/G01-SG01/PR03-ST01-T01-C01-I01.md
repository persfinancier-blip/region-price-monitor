# Prompt — region-price-monitor.G01.SG01.PR03.ST01.T01.C01.I01

## Identity
Task: `G01.SG01.PR03.ST01.T01` — Compose normalized product and city inputs  
Parent: G01.SG01 / Issue #31

## Goal
Expose one source-neutral handoff containing canonical ProductSet + CitySet without coupling downstream logic to file-vs-PostgreSQL origin.

## Prerequisite contracts
- PR01.ST01.T01 ProductSet boundary;
- PR02.ST02.T01 city file source;
- PR02.ST03.T01 city PostgreSQL source.

## Read first
- `docs/development/G01/SG01/vertical.md`
- accepted SG01 source modules/interfaces
- `parser/core/cli.py`

## Input contract
A selected product source and selected city source whose adapters already return canonical sets.

## Required output
A small orchestration/loading boundary whose result is semantically:

```python
{
  "products": ProductSet,
  "cities": CitySet,
}
```

Downstream callers must not need source-specific branches to interpret the result.

## Actions
1. Compose existing accepted adapters; do not duplicate parsing/validation logic.
2. Keep source selection explicit through arguments/configuration at this boundary; full unattended/scheduler UX is SG06 scope.
3. Preserve current CLI behavior unless minimal integration is necessary, but do not route new CitySet into legacy region warming/profile gates.
4. Add tests for all source combinations: product file/city file, product file/city DB, product DB/city file, product DB/city DB.
5. Ensure failure of either source is explicit and cannot masquerade as a valid empty input bundle.

## Allowed write scope
- `parser/core/cli.py`
- optional `parser/core/inputs.py`
- SG01 source integration tests/fixtures

## Acceptance
- all four source combinations produce the same normalized interface;
- source origin does not leak into ProductSet/CitySet shape;
- no manual city warm/profile step is introduced;
- no network/collector/result behavior is implemented.

## Evidence
Return exact changed files, tests/results and examples of the four source combinations converging to the same interface.

## Stop / fail closed
If runtime integration requires changing marketplace parsing, legacy profile semantics or scheduler behavior, stop and report the parent-scope dependency rather than widening this Task.
