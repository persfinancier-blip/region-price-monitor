# G01.SG02.PR03.ST02.T01.C02.I01

## Supersedes
`G01.SG02.PR03.ST02.T01.C01.I01` as active Prompt for Task #55. C01 remains immutable history.

## Role
Execute repaired SG02 closure after whole-G01 interface finding. Prove one ProxyContext authority across current HTTP adapters and SG04 browser adaptation boundary.

## Goal
Close SG02 without incorrectly requiring hidden-browser traffic to transit through `curl_cffi`.

## Inputs
All accepted SG02 outputs, repaired Task #54 C02 boundary, active SG04 C02 contract and Issue #32.

## Required evidence
Prove:
1. minimum SG01 city fields create one authenticated/redacted ProxyContext;
2. `wb_dest` is irrelevant to transport construction;
3. `requests` and `curl_cffi` consume the same ProxyContext;
4. current WB/Ozon HTTP call paths are bound to their SG02 adapters;
5. SG04 hidden-browser path, if used later, receives a browser-specific adaptation derived from the same ProxyContext without independently sourcing raw proxy credentials;
6. no primary engine silently goes direct when ProxyContext is supplied;
7. proxy/auth/connect/timeout/HTTP failures remain explicit and secret-safe at SG02 HTTP boundary;
8. transport construction itself requires no legacy profile/cookies/browser warm-up;
9. SG02 does not own browser context/city verification/lifecycle semantics.

## Output Contract
Repaired acceptance map plus cross-SG proof:
`SG01 CityRecord -> SG02 ProxyContext -> SG03/SG04 network engines`, with SG04 browser adaptation explicitly compatible.

## Acceptance
- single proxy authority preserved;
- no curl_cffi-only constraint on hidden browser;
- no new CityRecord field;
- no direct fallback;
- no credential leak;
- later SG semantics remain outside SG02.

## Failure Boundary
If SG04 can only configure browser proxy by creating an independent raw credential source, return `CROSS_SG_INTERFACE_MISMATCH`. Do not widen SG02 into browser/session implementation.