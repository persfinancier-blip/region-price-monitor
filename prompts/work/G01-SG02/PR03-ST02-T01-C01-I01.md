# G01.SG02.PR03.ST02.T01.C01.I01

## Goal
Produce deterministic closure evidence for every SG02 acceptance clause and reject hidden direct-network or credential-leak regressions.

## Inputs
All accepted SG02 Task outputs, Issue #32, `docs/development/G01/SG02/vertical.md`, current collector boundary tests.

## Required evidence
Prove:
1. the minimum SG01 city fields create authenticated proxy transport;
2. `wb_dest` is irrelevant to transport construction;
3. both `requests` and `curl_cffi` consume the same ProxyContext contract;
4. both WB and Ozon collector network call paths are bound to that transport;
5. proxy auth/connect/timeout/HTTP failures are explicit and never represented as empty valid marketplace data;
6. no raw proxy password/authenticated URI appears in ordinary logs, safe diagnostics, exceptions exposed to callers, or result-shaped objects;
7. transport construction/use needs no browser warm-up, profile directory or cookies file;
8. no silent direct-network fallback occurs when ProxyContext is supplied.

## Required output
Exact test commands/results plus an acceptance-to-evidence map and any cross-SG interface findings.

## Write boundary
Tests/fixtures and minimum fixes strictly inside SG02. Do not implement WB stock, optional-dest semantic behavior, Ozon automatic regional bootstrap, legacy fallback logic, persistence or scheduler.

## Stop/fail condition
Any finding outside SG02 must be routed to its owning Subgoal. If a Task boundary proves wrong, stop for canonical decomposition repair rather than widening this Prompt.
