# G01.SG03.PR01.ST01.T01.C05.I01

## Role
Correct the WB runtime-evidence direction after direct inspection of the owner-supplied working ZIP.

## Newly verified artifact fact
The uploaded `parser_wb_ozon_delivery` package is the July 27 working delivery and its relevant files match Git `main` exactly (same Git blob identities for `parser/core/wb.py`, `parser/core/config.py`, `parser/core/ozon.py`). Its WB price baseline is explicitly:

`https://card.wb.ru/cards/v4/detail`

with parameters `appType=1`, `curr=rub`, `dest`, `spp=30`, `nm` and the shipped WB headers.

Repository DEVLOG/spike evidence records that this v4 shape was live-working on 2026-07-22 and returned different prices for the same control SKU across Moscow and Vladivostok. Therefore there is no evidenced later hidden WB endpoint in this ZIP: the previously working replacement itself was v4.

## Current runtime contradiction
On 2026-08-11 through the proven SG02 Novosibirsk proxy context:
- v4 + archived Novosibirsk dest -> HTTP 400;
- v4 without dest -> HTTP 200 with `products=[]`.

Thus the task is no longer to search for another owner ZIP. The current WB request contract has changed or requires different current parameters/context.

## Goal
Recover the current WB **price** request by observing the live Wildberries frontend behavior for the control SKU and then replaying the observed request through the accepted SG02 Requests adapter. Browser/network observation is evidence discovery only; production price collection should remain a lightweight Requests path when a current machine endpoint is proven.

## Actions
1. Keep the July v4 request as historical baseline and provenance.
2. Through the same SG02 ProxyContext, load the official WB product page for the control SKU in a bounded browser/network-capture probe.
3. Capture only WB-owned request URLs/status/content-types that are triggered for that SKU; redact credentials and do not store unrelated browser data.
4. Identify the smallest current request that returns the requested SKU and price fields. Prefer an observed machine-readable JSON request; do not invent endpoint/parameters.
5. Replay the exact observed request using the SG02 Requests adapter and prove positive product/price evidence.
6. Preserve the shipped price parser semantics where compatible; record any exact schema delta.
7. Keep WB stock separate. Do not infer stock/availability from price and do not block price recovery on stock evidence.

## Output Contract
- `WB_CURRENT_PRICE_REQUEST_EVIDENCE_CAPTURED` with observed endpoint/parameter/schema provenance and Requests replay price evidence; or
- `WB_CURRENT_PRICE_REQUEST_UNPROVEN` with bounded browser/network evidence explaining why.

Stock remains independently `WB_STOCK_CONTRACT_UNPROVEN` until a later stock-specific evidence cycle.

## Acceptance
No guessed endpoint; official WB frontend observation is the evidence source; same SG02 ProxyContext; replay through Requests; no direct fallback; price and stock remain separate; no secrets in report/repository.

## Failure Boundary
Do not redesign Ozon/persistence/scheduler. Browser must not become the production WB price reader merely because it is used to discover the current request contract.
