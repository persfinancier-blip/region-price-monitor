# Immutable Prompt — G01.SG03.PR01.ST01.T01.C01.I01

## Task
Capture and classify the current Wildberries consumer-detail response shapes required to prove regional price and stock semantics before implementing a stock parser.

## Authority
- Parent Task: `G01.SG03.PR01.ST01.T01`
- Subgoal: Issue #33
- Contract: `docs/development/G01/SG03/vertical.md`
- Repository current WB path: `parser/core/wb.py`
- Accepted prerequisite when executed: SG02 shared proxy transport.

## Inputs
- representative WB SKUs from repository/test fixtures;
- at least one valid CityRecord/ProxyContext supplied through test/runtime configuration, never committed credentials;
- optional `wb_dest` case and no-forced-dest case.

## Actions
1. Read the exact current WB request path and SG02 transport contract.
2. Perform the smallest safe runtime probe(s) needed to observe current `cards/v4/detail` responses through the selected city ProxyContext.
3. Capture sanitized fixtures sufficient to represent:
   - a successful product with positive stock/availability signal;
   - a zero-stock/unavailable semantic case if obtainable;
   - a not-found/missing-product or malformed semantic case;
   - request with explicit `wb_dest`;
   - request without a forced `dest` parameter.
4. Record exact JSON paths carrying price and every stock/availability quantity/status used by the future parser.
5. If multiple sizes/stocks exist, record their structure without inventing an aggregation rule yet.
6. Remove proxy credentials, cookies, tokens, personal/IP-sensitive authentication material from durable fixtures/evidence.

## Required output
- sanitized WB response fixtures under the existing test-fixture convention or a minimal new fixture directory;
- a short schema/evidence note identifying exact paths observed;
- typed result `WB_STOCK_CONTRACT_UNPROVEN` if no trustworthy stock/availability signal can be established.

## Acceptance
- no stock field is inferred from price;
- evidence is tied to the current endpoint shape observed at execution time;
- with-dest and no-forced-dest request forms are distinguishable;
- no secrets are committed;
- inability to prove the schema blocks the next parser-semantic Task instead of being hidden.

## Tests / evidence
Record exact probe/test commands, sanitized response hashes/fixtures and observed HTTP outcomes.

## Forbidden scope
Do not implement the final stock parser, DB persistence, scheduler, Ozon, legacy browser fallback, or provider management.

## Stop condition
Enough sanitized current endpoint evidence exists to let `G01.SG03.PR01.ST02.T01` define stock normalization without guessing, or `WB_STOCK_CONTRACT_UNPROVEN` is emitted with evidence.
