# G01.SG03.PR01.ST01.T01.C03.I01

## Role
Repair only the current WB runtime-evidence probe after C02 proved the archived `card.wb.ru` request shapes no longer reproduce a product for control SKU `629760017`. Preserve C01/C02 as immutable history.

## Goal
Identify a current WB product/stock response shape through the already-proven SG02 ProxyContext without changing production parser semantics until evidence exists.

## Input
- accepted SG02 ProxyContext and Requests adapter;
- C02 live evidence: `card/v2` -> HTTP 404 with/without archived dest, `card/v4 + dest=-1075267` -> HTTP 400, `card/v4` without dest -> HTTP 200 + `products=[]`;
- archived diagnostic controls only: SKU `629760017`, Novosibirsk coordinates `55.0084,82.9357`, archived dest `-1075267`, archived `card/v1`, `u-card/v2` candidates and stock path `data.products[].sizes[].stocks[].qty`.

## Actions
1. Capture local bodies/hashes for non-2xx WB responses when a body exists; do not expose secrets in the safe report.
2. Probe the archived WB geo endpoint with the archived Novosibirsk coordinates through the same ProxyContext and record any currently returned dest/xinfo evidence.
3. Compare bounded endpoint candidates `card/v1`, `card/v2`, `card/v4`, `u-card/v2` with no dest and with the archived/currently observed diagnostic dest values when available.
4. For every successful JSON response, record product count, bounded price sample, and the exact observed stock/qty path and samples.
5. Do not interpret HTTP 200 + empty products as zero stock/not found.
6. Do not make archived coordinates/dest mandatory CityRecord fields; they are evidence controls only.

## Output Contract
Sanitized endpoint/dest comparison evidence sufficient to identify a current WB product/stock response shape, or fail closed `WB_STOCK_CONTRACT_UNPROVEN` / `WB_ENDPOINT_CONTRACT_UNPROVEN`.

## Acceptance
- one current live response with the requested product is required before stock-path semantics may advance;
- stock quantity is observed independently from price;
- no production parser change in this Prompt;
- no secret committed or printed in safe report;
- inability to reproduce a product remains a runtime evidence block.

## Failure Boundary
Do not implement final WB parser, persistence, retry policy, Ozon logic, or widen CityRecord.