# Immutable Prompt — G01.SG03.PR04.ST02.T01.C01.I01

## Task
Close G01.SG03 with deterministic evidence and reverse-assembly proof from Prompt outputs through Issue #33 into G01.

## Authority
Parent Task: `G01.SG03.PR04.ST02.T01`.
Subgoal: Issue #33.
Compile inputs: `docs/development/G01/SG03/vertical.md`, accepted outputs/evidence of all preceding SG03 Tasks, SG01 and SG02 accepted planning contracts.

## Required verification
Map exact evidence to every SG03 acceptance clause:
1. WB regional price is produced for successful requested product/city units;
2. stock/availability is parsed from a current endpoint-evidenced contract;
3. no stock-from-price inference remains;
4. positive and zero stock semantic fixtures pass;
5. zero stock is distinct from proxy/HTTP/parser failure;
6. explicit `wb_dest` request path passes;
7. missing `wb_dest` is accepted and `dest` is omitted rather than guessed;
8. both paths use the city SG02 ProxyContext with no silent direct fallback;
9. primary path has no manual WB `dest` acquisition prerequisite;
10. every requested SKU in a collector batch has a typed result/failure suitable for SG06 accounting;
11. SG03 has not absorbed persistence, scheduling, Ozon or fallback ownership.

## Virtual/reverse assembly
Compose:
`Prompt outputs -> Tasks -> Stages -> PR01..PR04 -> SG03 -> G01 requirements A05/A06/A08/A10/A12 contribution`.

Check interface compatibility with:
- SG01 `CityRecord` and WB ProductSet;
- SG02 ProxyContext/TransportOutcome;
- SG06 expected persistence/accounting consumer.

## Verdicts
- `SEMANTIC_COMPILE_PASS` only if the model is internally coherent and the runtime evidence prerequisite has a fail-closed path.
- `WB_STOCK_CONTRACT_UNPROVEN` if real endpoint stock semantics were not evidenced at execution time.
- `STRUCTURAL_REPAIR_REQUIRED` if findings require changing SG03/parent boundaries rather than another Prompt iteration.

## Forbidden behavior
Do not weaken tests, invent stock paths, make `wb_dest` mandatory again, or implement SG04-SG06 concerns to force PASS.

## Stop condition
One exact SG03 verdict and acceptance-to-evidence map exists, with unresolved runtime-only evidence clearly separated from semantic contract completeness.
