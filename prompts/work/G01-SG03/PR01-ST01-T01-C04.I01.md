# G01.SG03.PR01.ST01.T01.C04.I01

## Role
Repair WB evidence direction after owner correction. Do not continue endpoint archaeology from obsolete July spike URLs when a later working parser update is the intended source.

## Goal
Recover and reuse the exact WB **price** request shape from the later working parser artifact previously supplied by the owner, then validate it through the accepted SG02 ProxyContext. Keep WB stock as a separate evidence concern.

## Accepted current evidence
- SG02 ProxyContext egress is proven on Windows.
- Old archived candidates are stale under current runtime: v2 returned 404, v4+dest returned 400, v4 without dest returned HTTP 200 with empty products.
- The currently accessible delivery snapshot/Git main still contains `https://card.wb.ru/cards/v4/detail`; owner states a later working parser update replaced those card endpoints with working ones.

## Actions
1. Stop treating `card.wb.ru` v1/v2/v4 or `u-card/v2` from the old spike as candidate production price endpoints merely because they exist in repository history.
2. Recover the exact later working WB price implementation from the owner's working update artifact. Do not guess or substitute an internet-discovered endpoint when the intended source artifact is unavailable.
3. Once recovered, replay that exact price request through SG02 ProxyContext with the known control SKU and capture bounded current evidence.
4. Preserve the working price parser semantics from that artifact unless current evidence proves a specific incompatibility.
5. **Do not block price recovery on WB stock.** Stock/availability must be proved separately and may remain `WB_STOCK_CONTRACT_UNPROVEN` after price is restored.
6. Do not infer availability/stock from `price > 0`.
7. Record artifact provenance and exact request shape in evidence so this endpoint is not lost again.

## Output Contract
- exact recovered working WB price request shape + current price evidence through ProxyContext; or
- `WB_WORKING_PRICE_ARTIFACT_REQUIRED` if the later update artifact cannot be recovered exactly;
- independent stock status remains `WB_STOCK_CONTRACT_UNPROVEN` until separately evidenced.

## Acceptance
No guessed endpoint; price and stock concerns are separated; same SG02 ProxyContext authority; no direct fallback; secrets absent from evidence; old endpoint matrix is historical evidence only.

## Failure Boundary
Do not redesign Ozon, persistence, scheduler or invent WB stock semantics. Do not silently fall back to obsolete v1/v2/v4 request shapes.
